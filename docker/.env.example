# Copia questo file in docker/.env e personalizza le credenziali.
# docker/.env NON e' versionato (vedi .gitignore).
POSTGRES_DB=simulatore_luce
POSTGRES_USER=simulatore
POSTGRES_PASSWORD=cambia_questa_password
